{
  "name": "PROJECT_NAME",
  "version": "1.0.0",
  "description": "## Getting Started",
  "main": "index.js",
  "scripts": {
    "deploy-dev": "serverless deploy -v --profile profile",
    "deploy-homologa": "serverless deploy -v --profile profile",
    "deploy-production": "serverless deploy -v  --profile profile",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+GIT_REPO"
  },
  "devDependencies": {
    "serverless-domain-manager": "^2.6.13",
    "serverless-iam-roles-per-function": "^1.0.1"
  },
  "author": "",
  "license": "ISC",
  "homepage": "https://bitbucket.org/ioasys/yoube-backend#readme",
  "dependencies": {
    "aws-sdk": "^2.441.0",
    "gulp": "^4.0.1",
    "serveless": "0.0.2"
  }
}